{
  "name": "gitripp",
  "framework": "node",
  "isDualServer": true,

  "buildCommand": "cd frontend && npm ci && npm run build && mkdir -p ../backend/public && cp -r build/* ../backend/public/ && cd ../backend && dotnet publish -c Release -o ./publish",

  "startCommand": "cd backend/publish && dotnet GitXO.dll"
}