{
  "name": "gitripp",
  "framework": "node",

  "isDualServer": false,

  "buildCommand": "cd frontend && npm ci && npm run build && mkdir -p ../backend/public && cp -r build/* ../backend/public/ && cd ../backend && dotnet publish -c Release -o ../",

  "startCommand": "./GitXO.Api"
}